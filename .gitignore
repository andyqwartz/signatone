# Memory Bank (source de vérité cross-session — PAS versionné)
memory-bank/

# Superpowers docs (specs & plans — pas versionnés)
docs/superpowers/

# Environment
.DS_Store
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Build / generated
# note: js/alphabet.json is intentionally VERSIONED (runtime data needed on clone)

# Tuner dev tool (local tuning sandbox — not shipped)
tuner/